# This project is a React + TypeScript + Vite Chrome extension.
# It does not use Python packages, so there are no pip requirements to install.
# Use `npm install` to install the project dependencies listed in package.json.